import { e as createComponent, r as renderTemplate, k as renderComponent, m as maybeRenderHead } from '../chunks/astro/server_tVLEeqaO.mjs';
import 'kleur/colors';
import { $ as $$Layout } from '../chunks/Layout_CDAhgi8t.mjs';
export { renderers } from '../renderers.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a, _b;
const prerender = false;
const $$PdfToImages = createComponent(async ($$result, $$props, $$slots) => {
  const pageTitle = "\u062A\u062D\u0648\u064A\u0644 PDF \u0625\u0644\u0649 \u0635\u0648\u0631 - LOLME";
  return renderTemplate(_b || (_b = __template(["", ' <!-- ---\nexport const prerender = false;\nimport Layout from "../layouts/Layout.astro";\nconst pageTitle = "\u062A\u062D\u0648\u064A\u0644 PDF \u0625\u0644\u0649 \u0635\u0648\u0631 - LOLME";\n---\n\n<Layout title={pageTitle}>\n  <section style="padding: 2rem; text-align: center;">\n    <h1>\u062D\u0648\u0651\u0644 PDF \u0625\u0644\u0649 \u0635\u0648\u0631 \u{1F92F}</h1>\n    <p>\u0627\u062E\u062A\u0627\u0631 \u0645\u0644\u0641 PDF \u0648\u0647\u0646\u062D\u0648\u0651\u0644\u0647 \u0635\u0648\u0631 \u062A\u0642\u062F\u0631 \u062A\u062D\u0645\u0651\u0644\u0647\u0627 \u0643\u0644\u0651\u0647\u0627 \u0623\u0648 \u0643\u0644 \u0635\u0641\u062D\u0629 \u0644\u0648\u062D\u062F\u0647\u0627 \u{1F60E}</p>\n\n    <input type="file" id="pdfFile" accept="application/pdf" />\n    <button id="convertBtn">\u062D\u0648\u0651\u0644 PDF</button>\n\n    <div id="output" style="margin-top: 2rem;"></div>\n\n    <button id="downloadAllBtn" style="display:none; margin-top:1rem;">\n      \u062A\u062D\u0645\u064A\u0644 \u0643\u0644 \u0627\u0644\u0635\u0648\u0631 \u0643\u0640 ZIP \u{1F4E6}\n    </button>\n  </section>\n\n  <script type="module">\n    import * as pdfjsLib from "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.2.67/pdf.min.mjs";\n    import JSZip from "https://cdn.jsdelivr.net/npm/jszip@3.10.1/+esm";\n\n    pdfjsLib.GlobalWorkerOptions.workerSrc =\n      "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.2.67/pdf.worker.min.mjs";\n\n    const fileInput = document.getElementById("pdfFile");\n    const convertBtn = document.getElementById("convertBtn");\n    const outputDiv = document.getElementById("output");\n    const downloadAllBtn = document.getElementById("downloadAllBtn");\n\n    let imageUrls = [];\n\n    convertBtn.addEventListener("click", async () => {\n      const file = fileInput.files[0];\n      if (!file) {\n        alert("\u0627\u062E\u062A\u0627\u0631 \u0645\u0644\u0641 PDF \u0627\u0644\u0623\u0648\u0644 \u{1F605}");\n        return;\n      }\n\n      outputDiv.innerHTML = "<p>\u0628\u0646\u062D\u0648\u0651\u0644 \u0627\u0644\u0645\u0644\u0641... \u23F3</p>";\n      downloadAllBtn.style.display = "none";\n      imageUrls = [];\n\n      const arrayBuffer = await file.arrayBuffer();\n      const pdf = await pdfjsLib.getDocument({ data: arrayBuffer }).promise;\n\n      outputDiv.innerHTML = `<p>\u0627\u0644\u0645\u0644\u0641 \u0641\u064A\u0647 ${pdf.numPages} \u0635\u0641\u062D\u0629 \u{1F4C4}</p>`;\n\n      for (let i = 1; i <= pdf.numPages; i++) {\n        const page = await pdf.getPage(i);\n        const viewport = page.getViewport({ scale: 1.5 });\n\n        const canvas = document.createElement("canvas");\n        const context = canvas.getContext("2d");\n        canvas.width = viewport.width;\n        canvas.height = viewport.height;\n\n        await page.render({ canvasContext: context, viewport }).promise;\n\n        const imgData = canvas.toDataURL("image/png");\n        imageUrls.push(imgData);\n\n        // \u2705 \u0639\u0645\u0644 \u0643\u0627\u0631\u062A \u0644\u0643\u0644 \u0635\u0641\u062D\u0629\n        const card = document.createElement("div");\n        card.style.margin = "1rem 0";\n        card.style.padding = "1rem";\n        card.style.background = "#111";\n        card.style.border = "2px solid #0fe85b";\n        card.style.borderRadius = "10px";\n        card.style.display = "flex";\n        card.style.flexDirection = "column";\n        card.style.alignItems = "center";\n\n        const img = document.createElement("img");\n        img.src = imgData;\n        img.style.maxWidth = "90%";\n        img.style.borderRadius = "8px";\n\n        const downloadBtn = document.createElement("button");\n        downloadBtn.textContent = `\u062A\u062D\u0645\u064A\u0644 \u0635\u0641\u062D\u0629 ${i} \u2B07\uFE0F`;\n        downloadBtn.style.marginTop = "0.5rem";\n        downloadBtn.style.background = "#0fe85b";\n        downloadBtn.style.color = "#000";\n        downloadBtn.style.border = "none";\n        downloadBtn.style.padding = "0.5rem 1rem";\n        downloadBtn.style.borderRadius = "6px";\n        downloadBtn.style.cursor = "pointer";\n        downloadBtn.onclick = () => {\n          const link = document.createElement("a");\n          link.href = imgData;\n          link.download = `page-${i}.png`;\n          link.click();\n        };\n\n        card.appendChild(img);\n        card.appendChild(downloadBtn);\n        outputDiv.appendChild(card);\n      }\n\n      if (imageUrls.length > 0) {\n        downloadAllBtn.style.display = "inline-block";\n      }\n    });\n\n    // \u2705 \u062A\u062D\u0645\u064A\u0644 \u0643\u0644 \u0627\u0644\u0635\u0648\u0631 \u0643\u0640 ZIP \u0648\u0627\u062D\u062F\n    downloadAllBtn.addEventListener("click", async () => {\n      if (imageUrls.length === 0) return;\n\n      const zip = new JSZip();\n      const folder = zip.folder("pdf-images");\n\n      imageUrls.forEach((dataUrl, index) => {\n        const base64Data = dataUrl.split(",")[1];\n        folder.file(`page-${index + 1}.png`, base64Data, { base64: true });\n      });\n\n      const content = await zip.generateAsync({ type: "blob" });\n      const url = URL.createObjectURL(content);\n\n      const link = document.createElement("a");\n      link.href = url;\n      link.download = "pdf-images.zip";\n      link.click();\n\n      URL.revokeObjectURL(url);\n    });\n  <\/script>\n</Layout> -->'], ["", ' <!-- ---\nexport const prerender = false;\nimport Layout from "../layouts/Layout.astro";\nconst pageTitle = "\u062A\u062D\u0648\u064A\u0644 PDF \u0625\u0644\u0649 \u0635\u0648\u0631 - LOLME";\n---\n\n<Layout title={pageTitle}>\n  <section style="padding: 2rem; text-align: center;">\n    <h1>\u062D\u0648\u0651\u0644 PDF \u0625\u0644\u0649 \u0635\u0648\u0631 \u{1F92F}</h1>\n    <p>\u0627\u062E\u062A\u0627\u0631 \u0645\u0644\u0641 PDF \u0648\u0647\u0646\u062D\u0648\u0651\u0644\u0647 \u0635\u0648\u0631 \u062A\u0642\u062F\u0631 \u062A\u062D\u0645\u0651\u0644\u0647\u0627 \u0643\u0644\u0651\u0647\u0627 \u0623\u0648 \u0643\u0644 \u0635\u0641\u062D\u0629 \u0644\u0648\u062D\u062F\u0647\u0627 \u{1F60E}</p>\n\n    <input type="file" id="pdfFile" accept="application/pdf" />\n    <button id="convertBtn">\u062D\u0648\u0651\u0644 PDF</button>\n\n    <div id="output" style="margin-top: 2rem;"></div>\n\n    <button id="downloadAllBtn" style="display:none; margin-top:1rem;">\n      \u062A\u062D\u0645\u064A\u0644 \u0643\u0644 \u0627\u0644\u0635\u0648\u0631 \u0643\u0640 ZIP \u{1F4E6}\n    </button>\n  </section>\n\n  <script type="module">\n    import * as pdfjsLib from "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.2.67/pdf.min.mjs";\n    import JSZip from "https://cdn.jsdelivr.net/npm/jszip@3.10.1/+esm";\n\n    pdfjsLib.GlobalWorkerOptions.workerSrc =\n      "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.2.67/pdf.worker.min.mjs";\n\n    const fileInput = document.getElementById("pdfFile");\n    const convertBtn = document.getElementById("convertBtn");\n    const outputDiv = document.getElementById("output");\n    const downloadAllBtn = document.getElementById("downloadAllBtn");\n\n    let imageUrls = [];\n\n    convertBtn.addEventListener("click", async () => {\n      const file = fileInput.files[0];\n      if (!file) {\n        alert("\u0627\u062E\u062A\u0627\u0631 \u0645\u0644\u0641 PDF \u0627\u0644\u0623\u0648\u0644 \u{1F605}");\n        return;\n      }\n\n      outputDiv.innerHTML = "<p>\u0628\u0646\u062D\u0648\u0651\u0644 \u0627\u0644\u0645\u0644\u0641... \u23F3</p>";\n      downloadAllBtn.style.display = "none";\n      imageUrls = [];\n\n      const arrayBuffer = await file.arrayBuffer();\n      const pdf = await pdfjsLib.getDocument({ data: arrayBuffer }).promise;\n\n      outputDiv.innerHTML = \\`<p>\u0627\u0644\u0645\u0644\u0641 \u0641\u064A\u0647 \\${pdf.numPages} \u0635\u0641\u062D\u0629 \u{1F4C4}</p>\\`;\n\n      for (let i = 1; i <= pdf.numPages; i++) {\n        const page = await pdf.getPage(i);\n        const viewport = page.getViewport({ scale: 1.5 });\n\n        const canvas = document.createElement("canvas");\n        const context = canvas.getContext("2d");\n        canvas.width = viewport.width;\n        canvas.height = viewport.height;\n\n        await page.render({ canvasContext: context, viewport }).promise;\n\n        const imgData = canvas.toDataURL("image/png");\n        imageUrls.push(imgData);\n\n        // \u2705 \u0639\u0645\u0644 \u0643\u0627\u0631\u062A \u0644\u0643\u0644 \u0635\u0641\u062D\u0629\n        const card = document.createElement("div");\n        card.style.margin = "1rem 0";\n        card.style.padding = "1rem";\n        card.style.background = "#111";\n        card.style.border = "2px solid #0fe85b";\n        card.style.borderRadius = "10px";\n        card.style.display = "flex";\n        card.style.flexDirection = "column";\n        card.style.alignItems = "center";\n\n        const img = document.createElement("img");\n        img.src = imgData;\n        img.style.maxWidth = "90%";\n        img.style.borderRadius = "8px";\n\n        const downloadBtn = document.createElement("button");\n        downloadBtn.textContent = \\`\u062A\u062D\u0645\u064A\u0644 \u0635\u0641\u062D\u0629 \\${i} \u2B07\uFE0F\\`;\n        downloadBtn.style.marginTop = "0.5rem";\n        downloadBtn.style.background = "#0fe85b";\n        downloadBtn.style.color = "#000";\n        downloadBtn.style.border = "none";\n        downloadBtn.style.padding = "0.5rem 1rem";\n        downloadBtn.style.borderRadius = "6px";\n        downloadBtn.style.cursor = "pointer";\n        downloadBtn.onclick = () => {\n          const link = document.createElement("a");\n          link.href = imgData;\n          link.download = \\`page-\\${i}.png\\`;\n          link.click();\n        };\n\n        card.appendChild(img);\n        card.appendChild(downloadBtn);\n        outputDiv.appendChild(card);\n      }\n\n      if (imageUrls.length > 0) {\n        downloadAllBtn.style.display = "inline-block";\n      }\n    });\n\n    // \u2705 \u062A\u062D\u0645\u064A\u0644 \u0643\u0644 \u0627\u0644\u0635\u0648\u0631 \u0643\u0640 ZIP \u0648\u0627\u062D\u062F\n    downloadAllBtn.addEventListener("click", async () => {\n      if (imageUrls.length === 0) return;\n\n      const zip = new JSZip();\n      const folder = zip.folder("pdf-images");\n\n      imageUrls.forEach((dataUrl, index) => {\n        const base64Data = dataUrl.split(",")[1];\n        folder.file(\\`page-\\${index + 1}.png\\`, base64Data, { base64: true });\n      });\n\n      const content = await zip.generateAsync({ type: "blob" });\n      const url = URL.createObjectURL(content);\n\n      const link = document.createElement("a");\n      link.href = url;\n      link.download = "pdf-images.zip";\n      link.click();\n\n      URL.revokeObjectURL(url);\n    });\n  <\/script>\n</Layout> -->'])), renderComponent($$result, "Layout", $$Layout, { "title": pageTitle }, { "default": async ($$result2) => renderTemplate(_a || (_a = __template([" ", '<section style="padding: 2rem; text-align: center;"> <h1>\u062D\u0648\u0651\u0644 PDF \u0625\u0644\u0649 \u0635\u0648\u0631 \u{1F92F}</h1> <p>\u0627\u062E\u062A\u0627\u0631 \u0645\u0644\u0641 PDF \u0648\u0647\u0646\u062D\u0648\u0651\u0644\u0647 \u0635\u0648\u0631 \u062A\u0642\u062F\u0631 \u062A\u062D\u0645\u0651\u0644\u0647\u0627 \u0643\u0644\u0651\u0647\u0627 \u0623\u0648 \u0643\u0644 \u0635\u0641\u062D\u0629 \u0644\u0648\u062D\u062F\u0647\u0627 \u{1F60E}</p> <input type="file" id="pdfFile" accept="application/pdf"> <button id="convertBtn">\u062D\u0648\u0651\u0644 PDF</button> <div id="progressContainer" style="display:none; margin-top: 1rem;"> <div id="progressBar" style="width:0%; height: 20px; background:#0fe85b; border-radius:10px;"></div> <p id="progressText">0 / 0 \u0635\u0641\u062D\u0627\u062A</p> </div> <div id="output" style="margin-top: 2rem;"></div> <button id="downloadAllBtn" style="display:none; margin-top:1rem;">\n\u062A\u062D\u0645\u064A\u0644 \u0643\u0644 \u0627\u0644\u0635\u0648\u0631 \u0643\u0640 ZIP \u{1F4E6}\n</button> </section> <script type="module">\n    import * as pdfjsLib from "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.2.67/pdf.min.mjs";\n    import JSZip from "https://cdn.jsdelivr.net/npm/jszip@3.10.1/+esm";\n\n    pdfjsLib.GlobalWorkerOptions.workerSrc =\n      "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.2.67/pdf.worker.min.mjs";\n\n    const fileInput = document.getElementById("pdfFile");\n    const convertBtn = document.getElementById("convertBtn");\n    const outputDiv = document.getElementById("output");\n    const downloadAllBtn = document.getElementById("downloadAllBtn");\n    const progressContainer = document.getElementById("progressContainer");\n    const progressBar = document.getElementById("progressBar");\n    const progressText = document.getElementById("progressText");\n\n    let imageUrls = [];\n\n    function updateProgress(current, total) {\n      const percent = Math.round((current / total) * 100);\n      progressBar.style.width = percent + "%";\n      progressText.textContent = `${current} / ${total} \u0635\u0641\u062D\u0627\u062A`;\n    }\n\n    convertBtn.addEventListener("click", async () => {\n      const file = fileInput.files[0];\n      if (!file) {\n        alert("\u0627\u062E\u062A\u0627\u0631 \u0645\u0644\u0641 PDF \u0627\u0644\u0623\u0648\u0644 \u{1F605}");\n        return;\n      }\n\n      outputDiv.innerHTML = "";\n      downloadAllBtn.style.display = "none";\n      imageUrls = [];\n\n      progressContainer.style.display = "block";\n\n      const arrayBuffer = await file.arrayBuffer();\n      const pdf = await pdfjsLib.getDocument({ data: arrayBuffer }).promise;\n\n      const totalPages = pdf.numPages;\n      updateProgress(0, totalPages);\n\n      for (let i = 1; i <= totalPages; i++) {\n        const page = await pdf.getPage(i);\n        const viewport = page.getViewport({ scale: 1.5 });\n\n        const canvas = document.createElement("canvas");\n        const context = canvas.getContext("2d");\n        canvas.width = viewport.width;\n        canvas.height = viewport.height;\n\n        await page.render({ canvasContext: context, viewport }).promise;\n\n        const imgData = canvas.toDataURL("image/png");\n        imageUrls.push(imgData);\n\n        const card = document.createElement("div");\n        card.style.margin = "1rem 0";\n        card.style.padding = "1rem";\n        card.style.background = "#111";\n        card.style.border = "2px solid #0fe85b";\n        card.style.borderRadius = "10px";\n        card.style.display = "flex";\n        card.style.flexDirection = "column";\n        card.style.alignItems = "center";\n\n        const img = document.createElement("img");\n        img.src = imgData;\n        img.style.maxWidth = "90%";\n        img.style.borderRadius = "8px";\n\n        const downloadBtn = document.createElement("button");\n        downloadBtn.textContent = `\u062A\u062D\u0645\u064A\u0644 \u0635\u0641\u062D\u0629 ${i} \u2B07\uFE0F`;\n        downloadBtn.style.marginTop = "0.5rem";\n        downloadBtn.style.background = "#0fe85b";\n        downloadBtn.style.color = "#000";\n        downloadBtn.style.border = "none";\n        downloadBtn.style.padding = "0.5rem 1rem";\n        downloadBtn.style.borderRadius = "6px";\n        downloadBtn.style.cursor = "pointer";\n        downloadBtn.onclick = () => {\n          const link = document.createElement("a");\n          link.href = imgData;\n          link.download = `page-${i}.png`;\n          link.click();\n        };\n\n        card.appendChild(img);\n        card.appendChild(downloadBtn);\n        outputDiv.appendChild(card);\n\n        updateProgress(i, totalPages);\n        await new Promise(r => setTimeout(r, 50)); // \u062A\u062D\u0643\u0645 \u0628\u0633\u064A\u0637 \u0644\u062A\u062D\u062F\u064A\u062B progress\n      }\n\n      if (imageUrls.length > 0) {\n        downloadAllBtn.style.display = "inline-block";\n      }\n    });\n\n    downloadAllBtn.addEventListener("click", async () => {\n      if (imageUrls.length === 0) return;\n\n      const zip = new JSZip();\n      const folder = zip.folder("pdf-images");\n\n      imageUrls.forEach((dataUrl, index) => {\n        const base64Data = dataUrl.split(",")[1];\n        folder.file(`page-${index + 1}.png`, base64Data, { base64: true });\n      });\n\n      const content = await zip.generateAsync({ type: "blob" });\n      const url = URL.createObjectURL(content);\n\n      const link = document.createElement("a");\n      link.href = url;\n      link.download = "pdf-images.zip";\n      link.click();\n\n      URL.revokeObjectURL(url);\n\n      alert("\u062A\u0645 \u0625\u0646\u0634\u0627\u0621 ZIP \u0628\u0646\u062C\u0627\u062D! \u{1F525}");\n    });\n  <\/script> '], [" ", '<section style="padding: 2rem; text-align: center;"> <h1>\u062D\u0648\u0651\u0644 PDF \u0625\u0644\u0649 \u0635\u0648\u0631 \u{1F92F}</h1> <p>\u0627\u062E\u062A\u0627\u0631 \u0645\u0644\u0641 PDF \u0648\u0647\u0646\u062D\u0648\u0651\u0644\u0647 \u0635\u0648\u0631 \u062A\u0642\u062F\u0631 \u062A\u062D\u0645\u0651\u0644\u0647\u0627 \u0643\u0644\u0651\u0647\u0627 \u0623\u0648 \u0643\u0644 \u0635\u0641\u062D\u0629 \u0644\u0648\u062D\u062F\u0647\u0627 \u{1F60E}</p> <input type="file" id="pdfFile" accept="application/pdf"> <button id="convertBtn">\u062D\u0648\u0651\u0644 PDF</button> <div id="progressContainer" style="display:none; margin-top: 1rem;"> <div id="progressBar" style="width:0%; height: 20px; background:#0fe85b; border-radius:10px;"></div> <p id="progressText">0 / 0 \u0635\u0641\u062D\u0627\u062A</p> </div> <div id="output" style="margin-top: 2rem;"></div> <button id="downloadAllBtn" style="display:none; margin-top:1rem;">\n\u062A\u062D\u0645\u064A\u0644 \u0643\u0644 \u0627\u0644\u0635\u0648\u0631 \u0643\u0640 ZIP \u{1F4E6}\n</button> </section> <script type="module">\n    import * as pdfjsLib from "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.2.67/pdf.min.mjs";\n    import JSZip from "https://cdn.jsdelivr.net/npm/jszip@3.10.1/+esm";\n\n    pdfjsLib.GlobalWorkerOptions.workerSrc =\n      "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.2.67/pdf.worker.min.mjs";\n\n    const fileInput = document.getElementById("pdfFile");\n    const convertBtn = document.getElementById("convertBtn");\n    const outputDiv = document.getElementById("output");\n    const downloadAllBtn = document.getElementById("downloadAllBtn");\n    const progressContainer = document.getElementById("progressContainer");\n    const progressBar = document.getElementById("progressBar");\n    const progressText = document.getElementById("progressText");\n\n    let imageUrls = [];\n\n    function updateProgress(current, total) {\n      const percent = Math.round((current / total) * 100);\n      progressBar.style.width = percent + "%";\n      progressText.textContent = \\`\\${current} / \\${total} \u0635\u0641\u062D\u0627\u062A\\`;\n    }\n\n    convertBtn.addEventListener("click", async () => {\n      const file = fileInput.files[0];\n      if (!file) {\n        alert("\u0627\u062E\u062A\u0627\u0631 \u0645\u0644\u0641 PDF \u0627\u0644\u0623\u0648\u0644 \u{1F605}");\n        return;\n      }\n\n      outputDiv.innerHTML = "";\n      downloadAllBtn.style.display = "none";\n      imageUrls = [];\n\n      progressContainer.style.display = "block";\n\n      const arrayBuffer = await file.arrayBuffer();\n      const pdf = await pdfjsLib.getDocument({ data: arrayBuffer }).promise;\n\n      const totalPages = pdf.numPages;\n      updateProgress(0, totalPages);\n\n      for (let i = 1; i <= totalPages; i++) {\n        const page = await pdf.getPage(i);\n        const viewport = page.getViewport({ scale: 1.5 });\n\n        const canvas = document.createElement("canvas");\n        const context = canvas.getContext("2d");\n        canvas.width = viewport.width;\n        canvas.height = viewport.height;\n\n        await page.render({ canvasContext: context, viewport }).promise;\n\n        const imgData = canvas.toDataURL("image/png");\n        imageUrls.push(imgData);\n\n        const card = document.createElement("div");\n        card.style.margin = "1rem 0";\n        card.style.padding = "1rem";\n        card.style.background = "#111";\n        card.style.border = "2px solid #0fe85b";\n        card.style.borderRadius = "10px";\n        card.style.display = "flex";\n        card.style.flexDirection = "column";\n        card.style.alignItems = "center";\n\n        const img = document.createElement("img");\n        img.src = imgData;\n        img.style.maxWidth = "90%";\n        img.style.borderRadius = "8px";\n\n        const downloadBtn = document.createElement("button");\n        downloadBtn.textContent = \\`\u062A\u062D\u0645\u064A\u0644 \u0635\u0641\u062D\u0629 \\${i} \u2B07\uFE0F\\`;\n        downloadBtn.style.marginTop = "0.5rem";\n        downloadBtn.style.background = "#0fe85b";\n        downloadBtn.style.color = "#000";\n        downloadBtn.style.border = "none";\n        downloadBtn.style.padding = "0.5rem 1rem";\n        downloadBtn.style.borderRadius = "6px";\n        downloadBtn.style.cursor = "pointer";\n        downloadBtn.onclick = () => {\n          const link = document.createElement("a");\n          link.href = imgData;\n          link.download = \\`page-\\${i}.png\\`;\n          link.click();\n        };\n\n        card.appendChild(img);\n        card.appendChild(downloadBtn);\n        outputDiv.appendChild(card);\n\n        updateProgress(i, totalPages);\n        await new Promise(r => setTimeout(r, 50)); // \u062A\u062D\u0643\u0645 \u0628\u0633\u064A\u0637 \u0644\u062A\u062D\u062F\u064A\u062B progress\n      }\n\n      if (imageUrls.length > 0) {\n        downloadAllBtn.style.display = "inline-block";\n      }\n    });\n\n    downloadAllBtn.addEventListener("click", async () => {\n      if (imageUrls.length === 0) return;\n\n      const zip = new JSZip();\n      const folder = zip.folder("pdf-images");\n\n      imageUrls.forEach((dataUrl, index) => {\n        const base64Data = dataUrl.split(",")[1];\n        folder.file(\\`page-\\${index + 1}.png\\`, base64Data, { base64: true });\n      });\n\n      const content = await zip.generateAsync({ type: "blob" });\n      const url = URL.createObjectURL(content);\n\n      const link = document.createElement("a");\n      link.href = url;\n      link.download = "pdf-images.zip";\n      link.click();\n\n      URL.revokeObjectURL(url);\n\n      alert("\u062A\u0645 \u0625\u0646\u0634\u0627\u0621 ZIP \u0628\u0646\u062C\u0627\u062D! \u{1F525}");\n    });\n  <\/script> '])), maybeRenderHead()) }));
}, "/Users/khaledarafa/lolme/src/pages/pdf-to-images.astro", void 0);

const $$file = "/Users/khaledarafa/lolme/src/pages/pdf-to-images.astro";
const $$url = "/pdf-to-images";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$PdfToImages,
  file: $$file,
  prerender,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
