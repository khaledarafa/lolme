---
export const prerender = false;
import Layout from "../layouts/Layout.astro";
const pageTitle = "تحويل الصور إلى PDF";
---

<Layout title={pageTitle}>
  <section class="tool">
    <h1>📸 حول صورك إلى PDF</h1>

    <input type="file" id="imageInput" accept="image/*" multiple />

    <div class="options">
      <label>📏 حجم الصورة:
        <select id="imgSize">
          <option value="small">صغير</option>
          <option value="medium" selected>متوسط</option>
          <option value="large">كبير</option>
        </select>
      </label>

      <label>📄 الاتجاه:
        <select id="orientation">
          <option value="p" selected>طولي</option>
          <option value="l">عرضي</option>
        </select>
      </label>
    </div>

    <div id="previewContainer" class="preview-container"></div>

    <button id="convertBtn">تحويل إلى PDF</button>
  </section>

  <!-- ✅ تحميل مكتبة Sortable -->
  <script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.2/Sortable.min.js"></script>
  <!-- ✅ سكربت المنطق -->
  <script type="module" src="/js/images-to-pdf.js"></script>

  <style>
    .tool {
      text-align: center;
      padding: 1rem;
    }

    .options {
      margin: 1rem 0;
      display: flex;
      justify-content: center;
      gap: 1rem;
      flex-wrap: wrap;
    }

    select {
      padding: 5px;
      border-radius: 6px;
      border: 1px solid #ccc;
    }

    .preview-container {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 15px;
      justify-content: center;
    }

    .preview-item {
      position: relative;
      width: 100px;
      height: 100px;
      border: 2px solid #ddd;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
      background: #f8f8f8;
      transition: transform 0.2s ease;
    }

    .preview-item:hover {
      transform: scale(1.05);
    }

    .preview-item img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    .delete-btn {
      position: absolute;
      top: 5px;
      right: 5px;
      background: rgba(0, 0, 0, 0.6);
      color: white;
      border: none;
      border-radius: 50%;
      width: 22px;
      height: 22px;
      font-size: 14px;
      cursor: pointer;
    }

    input[type="file"] {
      margin-top: 10px;
      padding: 8px;
    }

    button#convertBtn {
      margin-top: 20px;
      padding: 10px 20px;
      background-color: #0fe85b;
      border: none;
      color: white;
      border-radius: 6px;
      cursor: pointer;
      font-size: 16px;
    }

    button#convertBtn:hover {
      background-color: #0dc24e;
    }
  </style>
</Layout>
